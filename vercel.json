{
  "version": 2,
  "builds": [
    {
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm run build" ,
        "outputDirectory": "dist",
        "installCommand": "npm install"
      }
    }
  ]
}
