{
  "version": 2,
  "builds": [
    {
      "src": "./.*",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm install && npx prisma generate && nest build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/main.js",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
    }
  ]
}