{
  "version": 2,
  "builds": [
    {
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm run build" ,
        "output": "dist",
        "installCommand": "npm install"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/main.js", 
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
    }
  ]
}
